<div data-commentid="{{ comment.id }}" class="fm_msg_item">
	<div style="margin:10px;">
		<div class="topic_illustration" style="float:left; width:60px">
			{% set avatar=userAvatar(comment.user) %}
			{% if avatar != "default.jpeg" %}
				{% set path='uploads/images/user/avatar/'~avatar %}
			{% else %}
				{% set path='default/images/user/avatar/'~avatar %}
			{% endif %}
			
			<img style="width: 60px; height: 60px; border-radius: 50%" src="{{ path | imagine_filter('60x60') }}" alt="{{ comment.user.username }}" />
		</div>
		<div style="margin-left:70px">
			<div>
				{# twig extension #}
				{% set userInfo = getLinkUserInfo(comment.user, comment.user.name) %}
				{{ userInfo | raw }}
				  on {{ comment.date | date('Y-m-d H:i:s') }}
			</div>
			<div>
				{{ comment.message | nl2br   }}
			</div>
		</div>
		<div class="both"></div>
	</div>
	<div class="sp mg_v10"></div>
</div>